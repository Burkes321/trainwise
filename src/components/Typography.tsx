import React from 'react';

import { Text } from 'react-native';

type Props = {
  text: string;
  fontSize: number;
};

// TODO: make a reusable typography component with a fixed number of sizes
export const Typography = ({ text }: Props) => <Text style={{}} />;
